/**
 * Child detail route under the private app shell (`/app`).
 * Full URL path: `/app/children/<childId>`.
 */
export function childDetailPagePath(childId: string) {
  return `/app/children/${childId}`;
}
